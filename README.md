PASSION4CODING-FRONTEND-TEST_TASK

Instructions for running/building the project:

To run this app:
`npm install`

then if you want dev server in hot reload:

`npm start`

for production:

`npm run build`